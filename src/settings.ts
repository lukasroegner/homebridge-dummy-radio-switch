
/**
 * The name of the platform that users will use to register the plugin in the Homebridge config.json.
 */
export const PLATFORM_NAME = 'DummyRadioSwitchPlatform';

/**
 * The name of the plugin as defined in the package.json `name` property.
 */
export const PLUGIN_NAME = 'homebridge-dummy-radio-switch';
